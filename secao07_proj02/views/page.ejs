<%- include('partials/header.ejs') %>
<%- include('partials/navbarHome.ejs') %>

<div class="container mt-5">
  <% result.articles.rows.forEach(article => { %>
    <div class="card mb-4">
      <div class="card-header">
        <h2><%= article.title %></h2>
      </div>

      <div class="card-body">
        <a href="/<%= article.slug %>" class="btn btn-primary">Ler artigo</a>
      </div>
    </div>
  <% }) %>

  <hr>
  
  <div class="row">
    <div class="col col-12">

      <% if (result.page > 2) { %>
      <a href="/articles/page/<%= result.page - 1 %>" class="btn btn-success mb-5">
        <<< Página Anterior
      </a>
      <% } else { %>
      <a href="/>" class="btn btn-success mb-5">
        <<< Página Anterior
      </a>
      <% } %>

      <% if (result.next) { %> 
      <a style="float: right;" href="/articles/page/<%= result.page + 1 %>" class="btn btn-success mb-5">
        Próxima página >>>
      </a>
      <% } %>
    </div>
  </div>
</div>

<%- include('partials/footer.ejs') %>